---
description: Use when working within the {{PLUGIN_NAME}} plugin directory.
globs: ["**/*"]
alwaysApply: false
---

# {{PLUGIN_NAME}}

このプラグインが提供するスキル・エージェント・コマンドの一覧は
プラグインルートの `AGENTS.md` を参照してください。

詳細な定義は `skills/<name>/SKILL.md`、`agents/<name>.md`、
`commands/<name>.md` の YAML frontmatter と Markdown 本体にあります。
