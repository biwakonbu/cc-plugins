---
description: カレントブランチを main にマージしてプッシュする
---

# Git Merge to Main

まず `git-conventions` スキルの規則を確認し、それに従った上で
`git-merge` スキルに従ってマージとプッシュを実行してください。

**注意**: このコマンドは main ブランチへのマージ・プッシュを意図しているため、
保護ブランチフックは自動的にバイパスされます。

引数: $ARGUMENTS
